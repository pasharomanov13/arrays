//число вхождений n  в массив
#include <math.h>
#include <iostream>
#define N 10
using namespace std;

int main()
{
        int A[N];
    int res = 0;
    int j=0;
    cin >> res ; //целое число n
    for (int a=0; a<N; a++)
    {
    cin >> A[a] ; //считываем 10 элементов массива
    }

    for (int i =0; i < N; i++)
    {
    if (A[i] == res) //сравниваем каждый эл. массива с целым числом, если совпало, увелич. счетчик
        j++;
    }
     cout << "number"<< j << endl;

    return 0;
}
